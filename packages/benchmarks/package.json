{
  "name": "benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "del-cli ./dist",
    "build": "vite build",
    "watch": "vite",
    "predev": "pnpm --filter react-native-web build",
    "dev": "pnpm --parallel --filter react-native-web --filter benchmarks watch",
    "preview": "vite preview"
  },
  "dependencies": {
    "classnames": "^2.5.1",
    "d3-scale-chromatic": "^3.1.0",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-native-web": "workspace:*"
  },
  "devDependencies": {
    "@types/d3-scale-chromatic": "^3.1.0",
    "@vitejs/plugin-react": "catalog:",
    "del-cli": "catalog:",
    "vite": "catalog:"
  }
}
